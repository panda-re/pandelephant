#! /usr/bin/python3

import argparse

# TODO

if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="Temp importer to test recursive syscall arg storage, to be replaced with Andre's implementation.")
    #parser.add_argument("-db_url", help="db url", action="store", required=True)
    parser.add_argument("-pandalog", help="pandalog", action="store", required=True)

